spring.datasource.hikari.initialization-fail-timeout=30000
spring.data.mongodb.auto-index-creation=false
spring.mongodb.uri=${SPRING_MONGODB_URI:mongodb://localhost:27017/acceptance_test}
app.auth.signing-key=${APP_AUTH_SIGNING_KEY:test-only-signing-key-at-least-32-bytes}
